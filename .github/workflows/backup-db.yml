# Postgres backup — daily pg_dump uploaded as a GitHub Actions artifact.
#
# Trigger policy: scheduled daily (02:00 UTC, between the two executor runs)
# plus workflow_dispatch for manual / ad-hoc runs. The schedule means the
# most-recent artifact is ≤24 h old at any point.
#
# What it does: pg_dump --no-owner --no-acl against DATABASE_PUBLIC_URL
# (Railway's external proxy URL — not the private internal URL), pipes
# through gzip, verifies the dump contains >=10 CREATE TABLE statements
# (so a silent empty/errored pg_dump — which can exit 0 — never uploads as a
# "backup"), then uploads as a 90-day artifact.  On failure it opens a bug
# issue so the maintainer is notified even if email notifications are off.
#
# Prerequisites (one-time owner setup):
#   1. Railway dashboard → Postgres service → Connect tab → copy the value
#      labelled "Database URL" under "Public networking" (this is the public
#      proxy URL, distinct from the internal DATABASE_URL).
#   2. GitHub repo → Settings → Secrets and variables → Actions →
#      New repository secret → name it DATABASE_PUBLIC_URL, paste the value.
#
# Restore procedure: docs/operations/production-deployment.md § Backups.
#
# Provenance + reliability (Q-0105): added 2026-06-13 (band slot 3).
# UNVERIFIED — confirm the first run produces an artifact and the dump is
# restorable (run a test restore to a local Postgres once the secret is set).
# Delete or disable this workflow if it proves unreliable.

name: Postgres backup (daily)

on:
  schedule:
    - cron: '0 2 * * *'  # daily 02:00 UTC — between the two executor runs (90-day artifact)
    - cron: '0 3 1 * *'  # monthly, 1st 03:00 UTC — long-retention tier (400-day artifact).
                         # Added 2026-07-02 (Q-0213 session): Railway-native volume backups are
                         # plan-gated on Hobby (API returns Not Authorized for schedule + manual
                         # create), so this workflow is the ONLY backup layer — the monthly tier
                         # protects against the 90-day artifact horizon.
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual backup (optional)'
        required: false

permissions:
  contents: read
  issues: write  # for the failure-notification issue

concurrency:
  group: postgres-backup
  cancel-in-progress: false  # never cancel an in-progress backup

jobs:
  backup:
    name: pg_dump → artifact (90 days)
    runs-on: ubuntu-latest
    if: github.repository == 'menno420/superbot'

    steps:
      - name: Verify secret is configured
        env:
          DATABASE_PUBLIC_URL: ${{ secrets.DATABASE_PUBLIC_URL }}
        run: |
          if [ -z "$DATABASE_PUBLIC_URL" ]; then
            echo "::error::DATABASE_PUBLIC_URL secret is not set."
            echo "Setup: Railway → Postgres → Connect → public URL → GitHub secret."
            echo "See docs/operations/production-deployment.md § Backups."
            exit 1
          fi
          echo "Secret configured."

      - name: Install postgresql-client (PGDG — must be >= the Railway server major)
        # Ubuntu's default postgresql-client is v16, but Railway's Postgres is v18,
        # and pg_dump refuses to dump a NEWER server with an OLDER client
        # ("aborting because of server version mismatch"). Add the official PGDG apt
        # repo and install the latest client — a newer client dumps older servers
        # fine, so a future Railway major bump won't re-break this.
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y postgresql-common
          sudo /usr/share/postgresql-common/pgdg/apt.postgresql.org.sh -y
          sudo apt-get install -y postgresql-client

      - name: Dump and compress
        id: dump
        env:
          DATABASE_PUBLIC_URL: ${{ secrets.DATABASE_PUBLIC_URL }}
        run: |
          TIMESTAMP=$(date -u '+%Y%m%dT%H%M%SZ')
          FILENAME="superbot-backup-${TIMESTAMP}.sql.gz"
          # The runner ships pg16 at /usr/bin/pg_dump, which shadows the PGDG client we
          # installed — so call the highest-version pg_dump by explicit path (future-proof:
          # picks 18 now, 19+ after a Railway major bump, as long as the client is installed).
          PG_BIN="$(ls -d /usr/lib/postgresql/*/bin 2>/dev/null | sort -V | tail -1)"
          echo "Using pg_dump: ${PG_BIN}/pg_dump"
          "${PG_BIN}/pg_dump" --no-owner --no-acl "$DATABASE_PUBLIC_URL" | gzip > "$FILENAME"
          SIZE=$(du -sh "$FILENAME" | cut -f1)
          echo "file=$FILENAME" >> "$GITHUB_OUTPUT"
          echo "Backup created: $FILENAME ($SIZE)"
          {
            echo "### Postgres backup"
            echo "- **File:** \`$FILENAME\`"
            echo "- **Size:** $SIZE (compressed)"
            echo "- **Retention:** 90 days (artifact)"
            echo ""
            echo "Download from the Actions run to restore."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Verify dump integrity
        # Runs BEFORE upload so a bad dump never occupies an artifact slot.
        # pg_dump can exit 0 yet emit a near-empty file (permission error,
        # wrong URL, truncated connection); the upload step's if-no-files-found
        # only checks existence/non-zero size, which a 1 KB error header passes.
        # Threshold 10 is a conservative floor; bump to ~(actual - 10%) after the
        # first verified run (the step prints the real count). (Q-0105: UNVERIFIED
        # until the first real dump is seen — delete if it proves flaky.)
        env:
          BACKUP_FILE: ${{ steps.dump.outputs.file }}
        run: |
          TABLE_COUNT=$(zcat "$BACKUP_FILE" | grep -c '^CREATE TABLE' || true)
          echo "CREATE TABLE statements in dump: $TABLE_COUNT"
          if [ "${TABLE_COUNT:-0}" -lt 10 ]; then
            echo "::error::Dump integrity check failed — only ${TABLE_COUNT:-0} CREATE TABLE statements (expected >=10). Refusing to upload a near-empty dump as a backup."
            exit 1
          fi
          echo "Dump integrity OK (>=10 tables)."
          echo "- **Tables:** $TABLE_COUNT (CREATE TABLE statements)" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          # The monthly schedule (1st, 03:00 UTC) uploads a long-retention artifact under a
          # distinct name; the daily keeps its 90-day rolling window. 400 days is GitHub's
          # maximum artifact retention — BUT GitHub silently clamps retention-days to the
          # repo's own maximum (Settings → Actions → General → "Artifact and log retention",
          # default 90). One-time owner step: raise that repo setting to 400, or the monthly
          # tier quietly keeps 90-day retention.
          name: postgres-backup-${{ github.event.schedule == '0 3 1 * *' && 'monthly-' || '' }}${{ github.run_id }}
          path: ${{ steps.dump.outputs.file }}
          retention-days: ${{ github.event.schedule == '0 3 1 * *' && 400 || 90 }}
          if-no-files-found: error

      - name: Open issue on failure
        if: failure()
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          script: |
            const today = new Date().toISOString().slice(0, 10);
            const runUrl = [
              context.serverUrl,
              context.repo.owner,
              context.repo.repo,
              'actions/runs',
              context.runId,
            ].join('/');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Postgres backup failed (${today})`,
              body: [
                'The scheduled Postgres backup workflow failed.',
                '',
                `**Run:** ${runUrl}`,
                '',
                '**Likely causes:**',
                '- `DATABASE_PUBLIC_URL` secret not set, or expired/rotated',
                '  (Railway updates it when the database is recreated)',
                '- pg_dump server/client version mismatch (Railway upgraded its',
                '  Postgres major — bump the postgresql-client install in this workflow)',
                '- Transient network issue between GitHub Actions and Railway',
                '',
                'Check the workflow logs. If the secret is stale, update it:',
                'Railway → Postgres → Connect → public URL → repo secret.',
                '',
                'See `docs/operations/production-deployment.md` § Backups for the',
                'full setup and restore procedure.',
              ].join('\n'),
            });
