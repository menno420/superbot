# Web dependencies for the developer dashboard service ONLY.
# Kept separate from the bot's root requirements.txt so CI's bot install and the
# bot's Railway build are unaffected (the dashboard is a separate Railway service).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
# httpx is only needed to run the test suite's TestClient (and, in later phases,
# to call the GitHub/Railway APIs); not required to serve the read-only MVP.
