# Consolidated question register — SuperBot rebuild foundational design (method step 7)

> **Status:** `reference` — **complete** (2026-07-04). **NOT SOURCE OF TRUTH.** This is an
> **owner-consumable** roll-up of every open decision the 14 foundational-design specs + the
> seam-consistency matrix + the adversarial sweep surfaced. Source specs win over this table
> (Q-0120); where a cell and a spec disagree, the spec is right. Durable owner answers route to
> `docs/owner/maintainer-question-router.md`; this doc is the single landing site that proves none
> of the design-session forks evaporated (V-3).

**Header — the numbers.** **31 decisions** · **23 owner-gated** 🔒 · **23 map to an existing FJ
owner-queue item / L-row / §4 gap**, **8 are net-new from this design session** (⊕). Tiers map to
**final-judgment §6** where a decision corresponds to one (T1/T2/T3 / A#/B#/C2-Q / L-row); the
cross-spec **seam reconciliations (F-1…F-5)** and mechanical **RC** additions have no FJ row and are
tiered by *what they block*. Reading order for the owner: **Tier-1 first** (five items gate the
strand-2 build + the vocab freeze), then the Tier-2 Gate-0 contract batch, then Tier-3 batch-bless.

**Two dedup notes (per the harvest brief).** (1) The **"atomic apply"** fork raised by *both* draft
(06) and workflow (07) is **one row — Q-D2**. (2) The **"one audit row vs N per compound apply"**
fork (07 fork B / 06 §③.3) is **design-CLOSED** — one central `audit_log` row + a nullable
`correlation_id = draft_id` DB-spine column; it is **not** an open decision. Its only open residual —
whether the **live** `audit.action_recorded` bus event *also* carries correlation for real-time
grouping — is folded into the retention fork **Q-D25 (SF-c)**.

---

## Tier 1 — resolve before the strand-2 build wires up / before the vocab re-freeze

Five cross-spec seams. Three are **mechanical / design co-decisions** (not owner-gated) but they are
*blocking*: built as written, two specs contradict and the artifact is unhosted or unrepresentable.
Two are owner-gated (the "atomic" wording leg of Q-D2; the intent posture Q-D5).

| # | Decision (one question) | Options | Recommendation (design-to-default) | Owning spec(s) | 🔒? | Maps to |
|---|---|---|---|---|:--:|---|
| **Q-D1** | **Who hosts the outbox relay?** 08 registers it as `OutboxRelayLane`+`OutboxReaperLane` (`PollLane`s on 09's `PollSupervisor`, 5 s); 09 says the relay is its own `RELAY_TASK(Interval 1s)`, "not my lane." Each cites the other as having decided the opposite ⇒ **built as written the relay is UNHOSTED** and cadence is 5 s vs 1 s. | (a) 09 hosts both outbox lanes on its `PollSupervisor` at 5 s (adopt 08's registered-lane model) · (b) standalone `RELAY_TASK` own loop · (c) leave contradictory | **(a)** — matches 09's own "one supervisor, registered lanes" principle; AT_LEAST_ONCE audit/reward tolerates ≤5 s. **09 edits §3.6/§11/§12.4** to host `OutboxRelayLane`+`OutboxReaperLane`, drops the "own `RELAY_TASK`/`Interval(1s)`/not-my-lane/self-reconcile" language, makes its lane roster non-exhaustive. | 08 ↔ 09 (**F-1**) | No | ⊕ net-new (durability seam; adversarial sweep raised it to **blocker** — under the AT_LEAST_ONCE audit default an unhosted relay = silent total loss of the operator audit trail on every restart) |
| **Q-D2** | **Atomic-apply semantics + which K7 entry the draft calls.** 07 provides `apply(op,*,conn)` (external-conn, no EFFECT legs) and names it "the draft entry"; 06 retired `apply(op)`, uses per-op `run(spec,ctx)` (own txn, runs the post-commit channel-create EFFECT legs its ops require), and disputes `run_ref`'s existence. Plus: is the resource-create lane still called **"atomic"**? | (a) reconcile to 06's per-op `run()` + EFFECT legs (draft = sequenced, per-op-atomic, idempotent-resume; **not** one shared txn) · (b) keep 07's shared-txn `apply(op,conn)` for the draft · **atomic wording:** keep vs **drop "atomic" for the resource lane** | **(a)** — 07 stops naming `apply(op,conn)` "the draft entry" (restrict it to pure-DB draft ops) and drops the fork-A shared-txn-for-draft claim; 06 updates its "`run_ref` not in 07's API" text (`run_ref`/`apply` exist, used by 09/scheduler). **Drop "atomic"** for the non-rollback-able resource lane; reserve all-or-nothing for pure-DB compound ops (A#1). | 06 ↔ 07 (**F-3**) | 🔒 (wording leg = T2-1/A#1; seam = design) | **T2-1** (A#1) |
| **Q-D3** | **Add `ActorRef.actor_type`?** 09's `SYSTEM_ACTOR` (`system`) and 11's `SWEEP_ACTOR` (`backfill`) need it so K7 maps `AuthorityRequest.actor_type` into `resolve_authority`'s scripted-bypass; frozen `ActorRef` (vocab §⑩) / spec 02 carry no such field. (Also: repair-actor value — reuse `backfill` vs a distinct `invariant_repair`, 11 Q5.) | (a) add `actor_type: str = "user"` to 02's `ActorRef` + vocab §⑩; repair reuses `backfill` · (b) leave it / thread differently | **(a)** — mechanical additive field, winner already determined (mirrors RC-12's `member_tier`); register as a **new RC** so 09/11 stop carrying it as a flagged correction. Repair reuses `backfill` for v1 (a distinct `invariant_repair` is an optional-additive forensic nicety, K6-scripted-set-touching ⇒ owner-gated only *if* taken). | 02 · 09 · 11 · vocab §⑩ (**F-5**) | No | ⊕ net-new (RC; sibling of RC-12) |
| **Q-D4** | **Which `Surface` member classifies a background fire's `from_exception`?** A scheduler fire / sweep-repair has no interaction origin; frozen `Surface` (RC-11) = {SLASH,PREFIX,COMPONENT,MODAL,NL_INTENT,NL_ORCHESTRATION} carries no background member. 09 uses `"scheduler"`; 11 reuses it for v1 but refuses to introduce a competing `"maintenance"` and flags it must be frozen for both siblings at once. | (a) reuse **`"scheduler"`** for both scheduler fires and sweep-repairs · (b) one broader **`"maintenance"`** member both siblings adopt | **(a) for v1** — one token, no fork; do NOT answer the same gap with two strings (that IS the drift the vocab kills). `"maintenance"` is a fine broader rename **iff** the vocab freezes it for 09 **and** 11 at once. Additive to 02's enum; register as a small **RC**. | 02 · 09 · 11 (**F-2**) | No (freeze-gated) | ⊕ net-new (RC; 11 §4 Q4) |
| **Q-D5** | **`IntentPosture`: DEGRADE or fail-closed?** 05 §3.1 declares `message_content`/`members` `required=True` ⇒ `FAILED_STARTUP` on an unapproved privileged intent (bot refuses to boot though every slash command would serve). 14 §2.B flips both to `required=False` + `IntentPosture.DEGRADE` (boot slash-only + explicit `DegradedCapability` + admin notice). | (a) **DEGRADE** (boot slash-only, explicit degrade, `required==(posture is REQUIRED)` mirror) · (b) keep 05's `required=True` fail-closed | **(a)** — fail-closed darks the whole bot when it could serve slash-only; slash-first survivability is the growth posture; the degrade preserves the fail-closed goal (no *silent* reliance) by being explicit. Route as a **spec 05 seam correction** (§3.1), owner-visible since it flips a frozen `required` field. | 14 → 05 (**F-4 / PG-2**) | 🔒 owner (touches frozen INTENT_CONTRACT) | **L-17** |

---

## Tier 2 — contract-level owner decisions, before the Gate-0 grammar freeze

Each has a **built default** so the build is unblocked today; the owner ratifies or overrides. All 19
are owner-gated 🔒.

| # | Decision (one question) | Options | Recommendation (design-to-default) | Owning spec(s) | 🔒? | Maps to |
|---|---|---|---|---|:--:|---|
| **Q-D6** | **Panel-action grammar (SF-a):** do panel actions/selectors funnel through `resolve()` carrying their own `authority_ref`/`enabled_when`/`reply_visibility`/`cooldown`/`defer_mode`, or keep a minimal `PanelActionSpec` with authority/cooldown **derived** from the owning panel? | (A) per-spec fields (route-through-C-1) · (B) minimal spec + derived path | **(A)** — retires L-5 ("component = 2nd resolver, no cooldown") *structurally*, one authority story per surface; (B) reintroduces a per-surface derivation the design exists to remove. Resolver contract is unchanged either way; 01 arms `never_strand`/`action_cooldown_parity` under (A). | 02 §8-a · 01 P6 | 🔒 | **L-5** |
| **Q-D7** | **Owner-override scope + transparency-sink wording (SF-b):** override authorizes in **member guilds only** or **any reachable guild**? Transparency notice → observability trace only, or a **distinct operator-visible** log? | scope (A) member-guild · (B) any-reachable; sink (i) observability-only · (ii) distinct operator-visible log + "would-not-otherwise-authorize" copy | scope **(A)** (built *structurally* — `owner_override_holds` requires `is_member`, X-7) + sink **(ii)** (bot-log + server-log, owner-DM digest fallback, **no durable row**). Owner confirms scope + names the copy. | 02 §8-b · 04 §8-a | 🔒 | **T2-10 / L-12** (A#9/A#10/C2-Q6) |
| **Q-D8** | **Store-drop `disposition` default (SF-g):** when a `StoreSpec` is dropped via a signed retirement, is there a global default disposition, or must every retirement name one? | (a) global default (e.g. `reverse-migrate`) · (b) **no default — `disposition` REQUIRED** per signed `store_retirements.yml` entry (`export`/`reverse-migrate`/`declared-loss`) · (c) default to safest | **(b)** — a silent global disposition is a silent data-loss path (the exact class §3.6 kills); a required field forces an explicit per-store call at sign-off. Compile fence forbids `DROP` on a value store regardless. | 01 §8 fork 8 | 🔒 (data-loss policy) | ⊕ net-new (parallels **L-18** rollback-disposition class) |
| **Q-D9** | **Which events are `delivery=AT_LEAST_ONCE` (08 OD-1 / 07 #7)?** Default is `BEST_EFFORT`; durable delivery adds outbox rows + a producer conn-threading refactor per opted-in event. | (a) audit + reward only · (b) audit + all economy/coin · (c) a broader owner-named set | **(a) the v1 set:** `audit.action_recorded`, `xp.awarded`, `xp.level_up`, `economy.balance_changed`. Every other event stays `BEST_EFFORT` (zero behavior change for the 30+ observability events). | 08 §13 · 07 §9-#7 | 🔒 | **T2-3 / L-9** |
| **Q-D10** | **`StoreSpec.version_policy` default for a `bears_value` store (09 OD-1):** when a value-bearing payload's schema drifts across a deploy with no `upcast_ref`, is the value preserved? | (a) `REJECT_AND_PRESERVE` (refund-before-delete) · (b) require an `UPCAST` path for every value store · (c) per-store override | **(a) `REJECT_AND_PRESERVE`** — never forfeit; the compile fence forbids `DROP` on a value store so the floor is safe regardless. Mechanism (all three policies + fence + `resolve_versioned_load`) is fully built either way; only the default is the call. | 09 §8.1 | 🔒 (data-loss) | **T2-7** (A#8; generalizes #1693) |
| **Q-D11** | **Invariant enforcement: permanent runtime sweep vs one-time migration script (11 Q1)?** | (A) permanent always-on sweep lane · (B) one-time verify-import script at cutover only · (C) permanent lane **report-only by default**, cutover verify-import hard-checks the same invariants | **(C)** — corruption is *continuous*, not a cutover event (recurs every deploy); (B) re-inherits the FJ §4 #7 blindness the day after swap; (A)'s auto-repair is real mutation risk. Build the permanent `PollLane`, ship every invariant report-only, flip individual ones to auto-repair as each proves out. | 11 §4 Q1 | 🔒 | **FJ §4 #7** |
| **Q-D12** | **verify-import (CUT-2) + verified-restore (CUT-3) as HARD cut gates or advisory (11 Q2 / 13 Q2)?** Two legs: backup-restorability precondition, and invariant-correctness granularity. | (A) HARD on all · (B) advisory scoreboard only · (C) **HARD on `bears_value` `RECONCILIATION`+`TERMINAL_ONCE`, advisory on the rest, owner-signed quarantine override**; backup-restorability leg = HARD | **(C)** for granularity (money/settle correctness hard-gates; the override keeps a false-positive from stranding the program) + **backup-restorability = HARD** (a backup never proven restorable isn't a backup; the continuous proof makes it rarely block). Decided **once** in 11 §4 Q2; 13 cross-refs, does not re-fork. | 11 §4 Q2 · 13 §4 Q2 | 🔒 | **L-18 / FJ §4 #7** (Q-0222 verify-import) |
| **Q-D13** | **Repair DIRECTION for a value-bearing violation — which store is ground truth (11 Q3, the money call)?** For `aggregate=500, ledger=480`: mint 20 ledger rows, or claw the aggregate to 480? | (A) aggregate = ground truth (launders an unaudited mint) · (B) ledger = ground truth (destroys real balance) · (C) **`QUARANTINE_ONLY`** — owner signs each disposition | **(C) as the built default** — the fence makes a repairable value-bearing reconciliation *require* an explicit `ground_truth_store`, so absent an owner-signed direction it ships `QUARANTINE_ONLY` and never auto-mutates money. The per-invariant direction (A vs B) is a genuine owner-only, **near-irreversible** money call. | 11 §4 Q3 | 🔒 (money, near-irreversible) | ⊕ net-new (money-repair direction; relates **L-1**) |
| **Q-D14** | **RPO target + source tier (13 Q1)** — how much data may a disaster lose, and via which backup source? | (A) daily `pg_dump` only ⇒ flat **≤24 h** for all stores incl. money/audit · (B) build an off-box `audit_log` export ⇒ minutes-RPO money spine (real build) · (C) Railway PITR ⇒ minutes-RPO whole-DB (plan upgrade) | **(A) is the honest built floor.** For minutes-RPO on the money spine: **(C) if the plan upgrade is acceptable, else (B).** Do **not** present near-continuous money RPO as free. Genuine owner call: plan-cost vs build-cost vs 24 h-acceptable. | 13 §4 Q1 | 🔒 (plan/build cost) | **L-18** (RPO leg) |
| **Q-D15** | **Rollback-data disposition + window N (13 Q3)** — what happens to writes made through the new bot during an N-day rollback window? | (A) pure `DECLARED_LOSS` + short N + sign-off · (B) `DECLARED_LOSS` **+ a narrow reverse-import valve for the derived `REVERSE_IMPORTABLE` (invertible-∧-value-bearing) tier** + short forward-fix-biased N + owner-signed M1/M2 loss manifest · (C) full reverse-import (**rejected** — impossible for collapsed/new-only stores) | **(B)** — round-trip the one class where loss is unacceptable (money/audit by `mutation_id` / absolute-value copy), declare the rest lost, keep N short and forward-fix-first. **Value of N stays the owner's Stage-3 carry.** Data-loss policy with a real reverse-importer build cost, near-irreversible either way. | 13 §4 Q3 · §2.4 | 🔒 (data-loss policy) | **L-18** + FJ §6 T2 rollback-disposition + Tier-3 N carry (line 359) |
| **Q-D16** | **A credential-lifecycle recovery arm at all (12 CL-1)?** — touches the Q-0213 credential concentration. | (a) full arm: registry + tiered cadence + revocation carve-out + runbook · (b) runbook only, no cadence · (c) none (accept no-kill-path) | **(a)** — a recovery arm is orthogonal to Q-0213 (Q-0213 declined *custody/scoping*, not *recovery*); it **removes** owner-dependency at compromise-time, the opposite of what Q-0213 guards. Route as router DISCUSS (touches a binding owner decision). | 12 §4 CL-1 | 🔒 | **FJ §4 #10** |
| **Q-D17** | **Revocation carve-out — narrow the Q-0213 `*Delete` brake to exclude a credential revoke (12 CL-2)?** | (a) credential revoke (`RevocationRef` closed set) = agent-runnable recovery; **resource** delete stays ask-first · (b) keep all `*Delete` ask-first (recovery waits on owner) | **(a)** — a token revoke loses no data; the brake's intent (Q-0213 item 4) is *data-loss* and its `*Delete` pattern over-captures `apiTokenDelete`. Route as router DISCUSS (narrows a Q-0213 brake). | 12 §4 CL-2 | 🔒 | **FJ §4 #10** (Q-0213) |
| **Q-D18** | **Supply-chain: add a lockfile + hash-verify + `pip-audit` CI gate, alongside the Q-0105 adopt-freely grant (12 CL-3)?** | (a) lockfile + CI gate; adopt-freely unchanged, the lock diff = the deferred-review artifact · (b) blocking human-review on every dep add · (c) status quo (floating `>=`, no lock) | **(a)** — enforces the runtime-pin rule Q-0105 already states; composes with adopt-freely (adopt → regenerate lock → CI verifies). Route as router DISCUSS (touches Q-0105 binding). | 12 §4 CL-3 | 🔒 | **FJ §4 #12** |
| **Q-D19** | **`SB_PROD_ATTEST` durable custody source (SF-d / 12 CL-5b):** keep the plain env token, or upgrade at CUT-1? | (a) presence-gated env `SecretSpec` (the 4th data-plane rail is correct today) · (b) sealed / managed secret · (c) short-lived OIDC claim | **defer to owner** — genuinely owner-gated (shared-vocab §⑧ SF-d). The spec is buildable now with (a); the rotation row homes in the credential registry (CL-5); only the custody *source* is carried forward unresolved. | 05 §9 · 12 CL-5b | 🔒 (ops / CUT-1) | **L-10** / FJ §4 #10 |
| **Q-D20** | **Adopt rubric classes 11/12/13 + run one adversarial-abuse pass (10 T-1/T-2/T-3)?** cost/quota (11), privacy/retention (12), security/non-functional (13); retroactive scope; who runs the pass. | classes: (a) adopt all three as-shaped · (b) different cut · (c) fold into audit-B's 5-facet. retro: (a) forward-only · (b) full re-walk · (c) forward + one retroactive pass. runner: (a) dedicated adversarial agent · (b) Stage-2 walkers · (c) owner | **classes (a)** (three orthogonal victim-axes: payer/data-subject/integrity, deterministically scoreable) + **retro (c)** (the pass *is* the retroactive coverage — one sweep, not 43 re-walks) + **runner (a)** (dedicated agent vs the frozen seams + Gate-0 grammar, rotated lenses). Rubric edits are proposed, not self-applied (Q-0233 froze the ten). | 10 §4 T-1/2/3 | 🔒 (rubric = owner-directed) | **L-19** / FJ §8 (Q-0233 candidate classes) |
| **Q-D21** | **Growth posture (14 PG-1):** pursue Discord verification as a **hard gate on growth**, or grow slash-first with an intent-denial fallback and verification pursued in parallel? | (a) slash-first survivability + intent-denial fallback; verification = a **parallel milestone, not a growth gate** · (b) hard-gate — freeze growth past ~75 guilds until `message_content` approved | **(a)** — verification approval is externally-owned + discretionary + latency-uncontrollable; a hard gate freezes the mission on Discord's queue, and `message_content` denial is routine. Slash-first is robust to every outcome; completes frozen Q-0237(e). | 14 §4 PG-1 | 🔒 | **L-17** / FJ §4 #1 |
| **Q-D22** | **Un-preserved-override disposition (14 PG-4):** RENAMED/DROPPED per-guild slash-permission overrides that can't be auto-restored (bot-token-un-writable). | (a) admin-notice + exact re-apply overlay · (b) per-guild admin OAuth2 grant + automated PUT-replay · (c) silent drop | **(a)** — never silently reset a guild's security config; (b) is a heavy integration barely better than a notice (still needs each admin's action); (c) is a privilege-escalation-at-migration risk. Lands in the CUT-3 comms plan. | 14 §4 PG-4 | 🔒 | **L-23** |
| **Q-D23** | **Deployment identity (14 PG-5):** reuse the same Discord application id at cutover, or a new application? | (a) **reuse the same app id** — un-renamed commands keep their ids ⇒ their overrides survive; only the enumerable rename/drop set needs a notice · (b) new application — every id changes ⇒ every override lost, none auto-restorable | **(a)** — shrinks the override-loss blast radius from "every override in every guild" to "only renamed/dropped commands," and is the difference between PG-3 being a light verify-gate vs a mass re-apply campaign. Choose (b) only if an ops constraint forces it. | 14 §4 PG-5 | 🔒 | ⊕ net-new (surfaced by the bot-token write limit) |
| **Q-D24** | **Multi-actor session concurrency-control: foundational kernel primitive, or Stage-2 per-subsystem?** No mechanism exists anywhere for concurrent session transitions (two joins as the window fires, a round-advance racing a forfeit, a giveaway draw racing entries, double-settle); 11's `TERMINAL_ONCE` is a post-hoc *detector*, not a preventer; the Gate-V multi-actor golden has nothing to pass against. | (A) name **K7 `NATURAL_KEY`-on-the-session-row** (FOR UPDATE / `WHERE state_version=N` compare-and-swap) as the designated seam for every multi-actor session transition + a compile fence requiring session-transition ops to declare it · (B) leave Stage-2, but add one coverage-map / strand-2 line stating the concurrency seam **is** K7 `NATURAL_KEY` and Stage-2 must use it | **(A)** — gives the Gate-V golden a mechanism to pass against; the primitive already exists (cited for escrow) but is un-connected to sessions and un-fenced. **Do NOT leave L-20 a test with no named mechanism.** | whole-set gap (adversarial find #2; 07 `NATURAL_KEY` · 11 `TERMINAL_ONCE`) | 🔒 (owner / architecture) | **L-20** |

---

## Tier 3 — Phase-B/C detail: defaults, retention, and bounded deferrals (batch-bless)

Low-blocking. The first four carry a real fork; the last three are chosen defaults / bounded
deferrals recorded so nothing evaporates.

| # | Decision (one question) | Options | Recommendation (design-to-default) | Owning spec(s) | 🔒? | Maps to |
|---|---|---|---|---|:--:|---|
| **Q-D25** | **Dispatch-trace / audit-retention promotion (SF-c):** keep `command.dispatched` (and the override-transparency notice, and draft-staging, and the live-bus draft-apply correlation) as `observability_only`, or promote to retained/correlated durable audit rows? | (A) observability-only / DB-spine-only for v1 · (B) promote to a retained `AuditEventSpec` (one row per dispatch) and/or add live-bus correlation | **(A)** for v1 — the per-mutation audit already covers auditable writes; a retained row per *dispatch* (or per override, or live-bus correlation for real-time grouping) is a **retention/volume** posture, not a correctness need. | 02 §8-c · 04 §8-c · 06 §9 · 07 §9-#6 | 🔒 (retention) | ⊕ net-new (feeds FJ §8 class 12 privacy/retention) |
| **Q-D26** | **`ChannelEmitter` send-egress port (10 T-7 / adversarial find #1):** extend the frozen K8 egress surface with a port for service-initiated `channel.send` (the X-1 mass-ping vector, reachable by no frozen primitive), or leave it un-primitived? | (a) new `ChannelEmitter` port + `OutboundContent`/`TrustLevel`, default-deny, AST-fenced (`raw channel.send outside adapters/discord/responders.py = SEMANTIC_VIOLATION`) · (b) leave un-primitived (X-1 stands) | **(a)** — the mass-ping vector is real shipped source; route as a **spec 02 / K8 seam correction** (owner-visible: it adds a port to a frozen `kernel/interaction` module), parallel to how RC-12/F-5 register the two `ActorRef` fields. | 10 §8.1 T-7 → 02/K8 | No (owner-visible seam correction) | **L-24** (allowed_mentions / mass-ping) |
| **Q-D27** | **CUT-2 permission census as a *binding* cutover gate (14 PG-3)?** No swap until the census is captured, the PRESERVED set carry-verified, and RENAMED/DROPPED enumerated into the admin-notice. | binding gate vs advisory | **Binding** — a rename that silently opens a locked-down moderation command is privilege-escalation-at-migration; the census is a bounded per-guild bot-token read. Decided by design (flagged). The one thing it can't bind is automated restoration of RENAMED/DROPPED overrides (platform-blocked → the notice, PG-4). | 14 §4 PG-3 | No (design-flagged) | **L-23** |
| **Q-D28** | **Test-mode effect routing (06):** under `test_mode`, suppress real Discord EFFECT writes, or route them to a real test guild? | (a) fail-safe **suppress** default · (b) route-to-test-guild for true end-to-end proof | **(a) suppress** as the safe built default (never touch a real guild); concrete test-guild routing is owned by the release-testing band + the L-10 test data-plane, behind the `AcceptHook` seam. Owner names the eventual test audience. | 06 §9 · retirement map | 🔒 (audience) | **L-10 / L-11** |
| **Q-D29** | **Durable cooldown store (SF-e):** merge=deploy-surviving cooldown state, or in-memory? | (a) in-memory (matches shipped; resets on restart) · (b) a durable `StoreSpec` backing the resolver's `CooldownSpec` read | **(a) for v1** (matches shipped behavior); if promoted it is a strand-2 durability `StoreSpec` the resolver reads off `CooldownSpec`. **Bounded deferral**, not a policy call. | 02 §9 | No | **T2-6 / L-8** |
| **Q-D30** | **Rung-4 NL-orchestration failure policy (SF-f):** stop-on-first-non-SUCCESS, or a per-plan continue/compensate policy? | (a) stop-on-first-non-SUCCESS default · (b) per-plan continue/compensate | **(a) default** — the sequential seam + shared `orchestration_id` are designed now; a per-plan policy is Phase-4 band 6 (the orchestration corpus isn't enumerated yet). **Bounded deferral.** | 02 §9 · 07 §9-#1 | No | ⊕ net-new (bounded deferral) |
| **Q-D31** | **Batch-ratify the design-decided seam refinements** (each has a recommendation, flagged only for owner awareness): rubric mechanization tags + Gate-0 binding + orthogonality/precedence (10 T-4/T-5/T-6) · `CredentialSpec` leaf + `check_credential_lifecycle` + SF-d rotation-row fold + `>=`-ceilings/`RevocationRef` freeze (12 CL-4/CL-5/CL-6) · repair `actor_type` value = reuse `backfill` (11 Q5) · de-repo-bind mechanics + retention step + integrity-floor comment fix (13 Q4). | adopt / amend | **Adopt all as recommended** — decided by design, contained + reversible; surfaced so Gate-0 ratifies them in one batch rather than re-deriving. | 10 · 11 · 12 · 13 | No | mixed (design-flagged) |

---

## Pointers

- **Inputs harvested:** the `open_decisions` / §8 "Options→Decision" / §4 "OWNER-GATED?" forks in all
  14 specs (`strand-1-kernel-spine/01–05`, `strand-2-runtime-durability/06–09`,
  `strand-3-cross-cutting-concerns/10–14`) · the five OPEN-FORKs (F-1…F-5) in
  `seam-consistency-matrix.md` · the shared-vocab §⑧ SF-fork table + RC ledger in
  `shared-vocabulary.md` · the adversarial-sweep REGISTER-disposition finds.
- **What is NOT here (closed, by design):** every §8 "forks I CLOSED" row; the audit-row-count fork
  (07 fork B / 06 §③.3 — one central row + `correlation_id` column); the `run_ref` external-conn
  seam (09 confirms it is settled, 07 §3.2 provides it); RC-1…RC-11 (reconciled) — RC-12…RC-15 are
  frozen additive corrections, the only two still *unlanded* being Q-D3 (`actor_type`) and Q-D4
  (background `Surface`).
- **Where answers go:** owner-gated rows → `docs/owner/maintainer-question-router.md` (DISCUSS lane
  where a row narrows a binding Q-0213/Q-0105/frozen-vocab decision); the 08 §13 / 09 §8.1 tables are
  the durable home for their own OD-1s once ruled.
