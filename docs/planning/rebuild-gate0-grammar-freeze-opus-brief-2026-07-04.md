# Opus 4.8 ultracode — the Gate-0 grammar-freeze + Phase-B L0 build-order session (brief)

> **Status:** `plan` — **preparation only** (owner-directed). A paste-ready **Opus 4.8 ultracode**
> prompt for the step after the foundational-design bridge (#1708): take the 14 buildable kernel
> specs + the frozen shared vocabulary + the question register, and (1) **freeze Gate-0** — consolidate
> every pinned grammar-field addition into ONE ratifiable grammar spec; (2) **resolve the register** —
> ratify the mechanically-decidable rows to their built defaults; (3) produce the **owner-decision
> packet** for the ~10 genuinely owner-only calls; (4) sequence the **Phase-B L0 build order**. Not
> launched from this session. Source wins over any doc (Q-0120).
>
> _(Appendix + THE PROMPT filled in during the prep session — this stub opens the born-red PR.)_
