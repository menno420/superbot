# Next-session brief — 2026-07-13: the FLEET REBOOT (website-served prompts) + new projects

> **Status:** `owner-guidance` — written at the owner's direction at the close of the 2026-07-12
> owner-live session (superbot #2043 + part-2 card). The owner's stated intent for the next
> session: **reboot ALL Projects on the same updated, centralized prompts/instructions served
> from the improved control website**, start a few new projects (one from the Ideas Lab's
> gathered material), and keep superbot-next + the games program moving.

## 1. The reboot — the pieces ALREADY exist; the sitting assembles them

The manager has been preparing exactly this. **Review-and-merge these first** (they are open,
owner-review-flagged, on fleet-manager):

- **fm #121** — "fleet consolidation + seat reset plan (owner-ordered)" — the reboot's plan of record.
- **fm #122** — "consolidation plan + prompt v3.4 restamp (owner review)" — the prompt bodies.
- (supporting: fm #118 v3.3 prompt-currency audit; fm #116 is a born-red hold, its session flips it.)

Then the sitting, in order:
1. **Publish the v3.4 prompt bodies to the control website** (websites seat: extend the existing
   prompt-library page): per-seat **assembled Custom Instructions** + **startup prompts** +
   the shared ender, each with a version stamp and a copy button. THE WEBSITE BECOMES THE PASTE
   SOURCE — no more raw-GitHub hunting. Add a per-seat "deployed vs canonical" drift row (ask the
   seat to quote its in-paste version line; compare to the site).
2. **The click procedure is the existing restructure queue trio** (fleet-manager owner-queue):
   `OQ-RESTRUCTURE-PROJECTS` (rename/create the 8 seats) → `OQ-RESTRUCTURE-INSTRUCTIONS-PASTE`
   (paste each assembled CI from the website) → `OQ-RESTRUCTURE-TRIGGER-CUTOVER` (paste each
   startup prompt as the first message; each boot rebinds its failsafe and retires the old
   triggers). One sitting, seat by seat.
3. **Bake the new discipline into the v3.4 bodies before pasting** (amendments this session earned):
   pacemaker **consume-before-re-arm, one outstanding tick, silent no-op wakes** (the court-spam
   class, ORDER 020 amendment); the **disconfirming-probe rule** before declaring any wall; the
   rescue-venue capabilities pointer.

## 2. New projects

- **The Ideas Lab candidate:** the owner says the idea project has gathered more info on one idea —
  next session: owner names it → the manager drafts its **founding package** from the
  next-round founding-prompt kit (`docs/owner/next-round-founding-prompts-2026-07-11.md`) →
  it boots as part of the same reboot sitting (new Project, website-served prompts from day one).
- Any further new seats follow the same path; keep the 8+N count deliberate — every new seat gets
  a mission + done-when or it doesn't boot (MISSION.md doctrine).

## 3. superbot-next — the #1 unblock is the merge wall

**16 open PRs** (mining slices 1–4, browse-surface arc, casino/Hold'em, parity goldens, cutover
runbook, deploy packaging…) are piled behind the 6-check + require-up-to-date ruleset
(`OQ-NEXT-MERGE-QUEUE`). Highest-value moves, in order:
1. Owner click: **enable the merge queue** on superbot-next (or relax up-to-date for docs/control)
   — turns the backlog self-draining. 2. A **drain sitting** (owner or the SuperBot 2.0 seat post-
   reboot): oldest-first, they are mostly green-waiting. 3. **PLUG-001 is UNPARKED** (idle #72:
   the plugin contract is verified, host implementation exists) → say the seed-push word
   (`OQ-PLUGIN-SEED-WORD`) to prove the contract end-to-end. 4. Then the canonical rebuild plan
   continues (CUT-2/CUT-3 runbook is already a PR in the pile).

## 4. superbot-games / SuperBot World — the flagship needs its data

Mineverse is LIVE with sign-in (today's milestone). The two banners left are **superbot bot-lane**
work — route as one order: **FLAG 1** (60s mining-snapshot relay; spec verbatim in mineverse
`control/status.md`) + **FLAG 2** (HMAC write endpoint; same doc). Done = real miners on the live
site + write mode in test guilds (the write-pair env vars get set the same day — agent-side).
games #59/#60 (docs PRs) merge in the drain sitting; idle #74 (CI workflow) likewise.

## 5. The rest of the open-PR sweep (dispositions)

- **websites #194** (bake data refresh) — merge-on-green class; #163 draft card — leave (record-only).
- **substrate-kit #220/#238** — pin-path, **ratify-by-design**: owner merges or rejects with a word.
- **gba #68/#69** (game slices) + **mineverse #31** (owner's Codex security report) — owner review clicks.
- **fm #135** (trigger-health remediation — the manager acting on ORDER 020) — merges on its own lane.

## 6. Standing items unchanged

Venture-lab publish **evidence** (if published today: drop the listing URL anywhere so the T+7/T+14
checkpoints — already armed — have a live target). The **07-14 bundled sitting** decisions
(`OQ-SITTING-0714-DECISIONS`). The **EAP window closes Tue 07-14** — the review site is refreshed
and AI-armed (ORDER 019 done); one last cold-browser pass before Tuesday is cheap insurance.
Railway project consolidation stays FROZEN until after 07-14 (`OQ-RAILWAY-PROJECT-SPLIT`).
