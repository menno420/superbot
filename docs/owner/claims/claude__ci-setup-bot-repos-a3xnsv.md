- `claude/ci-setup-bot-repos-a3xnsv` · **CI Phase-A safe build** — make the should-gate-but-doesn't invariants actually block merges via the existing `code-quality` required context (check_architecture --strict, check_tool_pins, check_workflow_concurrency) + flip codeql.yml cancel:false · `.github/workflows/code-quality.yml` `.github/workflows/codeql.yml` `docs/planning/` `docs/operations/` · 2026-07-05 · safe-additive (no branch-protection change)
