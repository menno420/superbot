- `claude/help-reachability-guard` · **Help reachability guard (orphan check)** (self-initiated, Q-0172) — a CI invariant + `--check` mode on the grouping sim that fails when a subsystem isn't homed under a hub / exceeds 3 clicks / overflows the dropdown page. Load-bearing now the Advanced catch-all is gone (#1294) · `tools/sim/help_menu_grouping_sim.py` `tests/unit/invariants/test_help_reachability.py` · 2026-06-22 · PR (this session, auto-merge on green)
