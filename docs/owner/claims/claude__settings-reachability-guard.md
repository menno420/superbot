- `claude/settings-reachability-guard` · **Phase-0.5 settings-orphan CI guard** — the consolidation-fleet-plan's specced rail: a warn-first ratchet (`scripts/check_settings_reachability.py` + invariant test + `architecture_rules/settings_reachability_exceptions.yml`) asserting every subsystem that *declares settings statically* has a *discovery path* (reuses `check_command_reachability._subsystem_discoverable`). Offline-safe (the live `customization_catalogue` is bot-walk-blind in CI). Unblocks the settings/admin fleet wave (U5–U9). Scope: `scripts/`, `tests/unit/invariants/`, `architecture_rules/`, docs. 2026-06-23 · PR (this session, auto-merge on green)
