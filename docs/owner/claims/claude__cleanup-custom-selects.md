- `claude/cleanup-custom-selects` · **Button/select-driven Custom cleanup level** (owner-directed) — replace the typing-based `_CustomLevelModal` (type yes/no/seconds) in the cleanup policy builder with select menus (delete-after duration picker + Yes/No selects), so configuring a custom policy needs no typing · `disbot/views/cleanup/policy_panel.py` + tests · 2026-06-23 · PR (this session, auto-merge on green)
