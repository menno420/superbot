# Claim

- `claude/superbot-rebuild-final-review-nezu89` · substrate-kit: adopt installs the ENFORCEMENT (the forcing functions that make the memory discipline actually get used) — a real session-gate that fails when the journal is missing + a live CI workflow that runs it + the live nag hook; then prove the locked door blocks→unblocks · expected files: `substrate-kit/src/engine/{cli.py,adopt.py,checks/}`, `substrate-kit/dist/bootstrap.py`, `tests/unit/substrate_kit/`, `docs/planning/` · owner-directed 2026-07-07 · 2026-07-07
