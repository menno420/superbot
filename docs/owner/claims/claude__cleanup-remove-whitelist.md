- `claude/cleanup-remove-whitelist` · **Remove the legacy cleanup channel whitelist** (owner-directed, follow-up to #1345) — the hardcoded `CLEANUP_WHITELIST_CHANNELS` (4 old-server IDs) is a remnant of an old bot version; per-channel cleanup policies (`Off`) fully replace it. Delete the config constant + `BOT_CLEANUP_WHITELIST` env hook + `_parse_channel_ids`, the resolver fallback, the cog's auto-mod fallback, and the Hub whitelist field · `disbot/config.py` `disbot/governance/cleanup.py` `disbot/cogs/cleanup_cog.py` `disbot/cogs/cleanup/panel.py` + tests/docs · 2026-06-23 · PR (this session, auto-merge on green)
