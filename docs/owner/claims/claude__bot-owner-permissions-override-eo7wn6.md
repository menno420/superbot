- `claude/bot-owner-permissions-override-eo7wn6` · **bot-owner override — completeness follow-up** — #1573 fixed the governance/service/setup seams but MISSED two gate classes the owner still hits: (1) the `views/ai/policy/*` + `views/ai/routing` + xp/roles config view `interaction_check`s, (2) every `@has_permissions(administrator=True)` cog command decorator (101 prefix + 28 slash). Route all views through the canonical helper; replace admin-only decorators with an owner-aware `admin_or_owner`/`app_admin_or_owner` check; add CI guards so neither class can be missed again. · `disbot/core/runtime/permission_checks.py` `disbot/cogs/*` `disbot/views/ai/**` `disbot/views/{xp,roles}/main_panel.py` · 2026-06-30 · in-progress (follow-up to merged #1573)
