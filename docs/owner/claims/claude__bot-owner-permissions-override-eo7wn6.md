- `claude/bot-owner-permissions-override-eo7wn6` · **owner bypasses ALL permission gates, not just administrator** — owner directive "make sure I can do everything with this bot as owner". #1573/#1577 covered `administrator`; the owner still hits **specific-permission** gates (`manage_roles` on role menus, `manage_guild`/`manage_channels`/`manage_messages`/`moderate_members`/`create_instant_invite`) — decorators + inline view/panel checks. Generalize the owner-aware helpers to any permission (`perms_or_owner`/`app_perms_or_owner`/`member_has_perms_or_owner`), sweep every `has_permissions(...)` decorator + inline `guild_permissions.<perm>` gate, and add a guard that enumerates ALL raw gates so none is missed. · `disbot/core/runtime/permission_checks.py` `disbot/cogs/*` `disbot/views/roles/*` + guard/tests · 2026-07-01 · in-progress
