- `claude/bot-owner-permissions-override-eo7wn6` · **fix: ephemeral persistent-panel ownership fail-close** — a `PersistentView` with `FAIL_CLOSED_ON_MISSING_ANCHOR=True` (e.g. `RoleHubPanelView`) denies its own opener ("This panel can no longer be verified — please re-open it") when surfaced through an **ephemeral** path (`!help`/`/help` → Roles via `build_help_menu_view`), because ephemeral messages never get an anchor row. Ephemeral messages are private to the invoker, so ownership is implicit → allow them in `interaction_check`. · `disbot/core/runtime/persistent_views.py` + regression test · 2026-06-30 · in-progress
