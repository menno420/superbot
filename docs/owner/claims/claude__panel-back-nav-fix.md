- `claude/panel-back-nav-fix` · **Panel back/help navigation-completeness fixes** (owner live-walk findings) — root-cause the class where (1) returning to a panel after an action drops the help/hub back-target (AI=fleet regression, rps/blackjack/mining) and (2) game-result screens dead-end with no continuation/back (deathmatch/fishing/casino). Thread the back-target through play→result→return paths + add back/replay to terminal views + per-panel regression tests. Scope: `disbot/views/ai/`, `disbot/views/games/`, per-game view dirs (`blackjack`/`rps`/`mining`/`fishing`/`casino`/`creature_battle`), `tests/`. Started with the AI regression. 2026-06-23 · PR (this session, auto-merge on green)
