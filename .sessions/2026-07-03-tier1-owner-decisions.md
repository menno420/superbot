# 2026-07-03 — Tier-1 owner decisions recorded (Q-0237)

> **Status:** `complete`

**Run type:** owner-directed (the owner engaged the question panel to answer the 7 Tier-1 decisions
the Fable-5 judgment, PR #1701, named as blocking Stage 2). **PR #1703.** Docs-only. Restarted fresh
from `origin/main` (the judgment PR #1701 had merged).

**Did:** recorded the **7 Tier-1 rebuild decisions** the owner answered live. Canonical home: new
router block **Q-0237(a–g)**; amendment pointers added to the three affected decision docs
(hub/nav-presets §1 admin + §3 preset sub-decision; conventions §1 naming + §4 authority; stage-1
§6 Stage-2 agenda); final-judgment §6 Tier-1 table marked RESOLVED.

**The 7 decisions:** (a) preset exclusion = visibility-only not disable; (b) Back = in-session real
stack + semantic-parent fallback after restart; (c) admin = hidden node inside the unified hub
(**amends Q-0230** "gated visible" → "hidden"); (d) one `authority_ref`; (e) slash-common + prefix
long-tail; (f) `!admin`/`!games` canonical, shipped `-menu` names become hidden aliases; (g) adopt
Codex-4's Stage-2 kit as-is. Six matched the judgment recommendation; (c) deviated. **Effect:
Stage-2 unblocked** alongside the RPS fee-forfeit fix (V-1) and findings-closure (V-3).

**⚑ Self-initiated:** none — recording owner-made decisions is the direct follow-through of the
sitting; the amendment to Q-0230 is the owner's own call (c), applied with provenance per the
in-session-owner-reviewer rule.

**💡 Session idea (Q-0089):** none new — this is a short capture session immediately after the
judgment session (which already contributed its idea, the fleet placeholder-guard). Per the Q-0089
bar, forced filler is worse than none; the judgment session's ender stands for this pair.

**⟲ Previous-session review (Q-0102):** the judgment session (#1701) tiered the owner queue into 7
Tier-1 rows with a recommendation each — which is exactly what made this sitting a fast one-pass
answer rather than a 50-item slog. What it could have done better: it left the 7 as a table inside
a 469-line doc rather than surfacing them as a ready-to-answer panel; **system improvement** — a
capstone judgment that produces owner-gated decisions should end by *offering the question panel*,
not just tabulating, so the answer loop closes in the same breath. (This session did that; worth
making it a standing capstone-session ender.)

**Docs audit (Q-0104):** `check_docs --strict` + `check_plan_homing` green; Q-0237 recorded in the
router with per-decision provenance and homes; the three decision docs carry amendment pointers so a
reader of the frozen doc sees the resolution; no chat-only decisions left unhomed.
