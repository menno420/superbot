# 2026-07-03 — Prepare the Opus-4.8 foundational-DESIGN overnight prompt

> **Status:** `in-progress`

**About to do:** author (NOT execute) a comprehensive, paste-ready **Opus 4.8 ultracode** prompt for
an overnight foundational-**design** session — the follow-through on the owner's "what did we not
properly plan, especially foundational functions" question. The overnight session's job: take the
foundational **functions** that were audited (audit A) but never **designed** — the manifest
compiler + snapshot (the linchpin), the C-1 resolver, the error/failure envelope, the C-2 draft
pipeline, the workflow/compound-op engine, the outbox/durable-delivery, the durable
scheduler/due-queue + versioned-state, K1 namespace, the authority engine, the ops kernel
(config/DB/migrations/metrics/readiness) — and **design each to buildable depth**; close the
never-surfaced foundational **concerns** (security/abuse + data-integrity + credential lifecycle;
backup/DR + rollback-data + platform-governance growth gate + multi-user verification); and
**surface every remaining question/decision**. It is a DESIGN + question-surfacing session, NOT a
fourth audit and NOT the Stage-2 subsystem walk.

**This session prepares only** — grounded the prompt with a 13-agent map of each function's current
state/seams/open-questions against shipped source (Q-0120) so the prompt cites real files, not
memory. Deliverable: `docs/planning/rebuild-foundational-design-opus-brief-2026-07-03.md`. Docs-only.
