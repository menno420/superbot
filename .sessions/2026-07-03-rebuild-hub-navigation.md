# 2026-07-03 — Rebuild Phase A · hub topology + navigation contract + interface presets (owner-live)

> **Status:** `in-progress` — owner-live continuation of the conventions freeze, capturing the
> hub/navigation/interface-customization decisions: one unified help hub (admin a gated node +
> `!admin` direct-open); the navigation contract (Back+Home injected into every state, persistent
> restart-safe panels, every node directly openable); and per-guild interface presets with live
> preview — verified as an EXISTING working surface (setup preset_select + help overlay editor +
> ~7 scattered preset impls) to be improved + centralized. Folding into a decisions log + router
> Q-blocks. Docs-only; no `disbot/` code.
