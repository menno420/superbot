# 2026-07-03 — Surface + proving foundations audit (PROMPT B, ultracode)

> **Status:** `in-progress` — born-red session card (holds auto-merge until the work + close-out
> docs land, Q-0133). **What I'm about to do:** run PROMPT B of the foundational-mechanics brief
> (`docs/planning/rebuild-foundational-mechanics-ultracode-brief-2026-07-03.md`) via an ultracode
> workflow — a docs-only brainstorm/audit of the **presentation/UX + verification** foundation of
> the SuperBot rebuild (hub topology + navigation engine, panel rendering, the card + media engine,
> interface presets/templates, help/description projection, the response/result grammar, the
> "did you mean" suggestion surface, the critical-review rubric itself, the correctness oracle, and
> the layout-success simulator). Deliverable: `docs/analysis/rebuild-discovery/foundations/
> presentation-verification-mechanics-2026-07-03.md` — a mechanics inventory + ranked issues ledger.
> No `disbot/` code, no bot launch; a parallel session runs PROMPT A (runtime/logic).

<!-- close-out docs (what shipped · idea · previous-session review · doc audit) get written here
     as the deliberate LAST step, then the Status badge flips to `complete` → CI green → auto-merge -->
