# 2026-07-09 — Dependabot PR policy (owner decision) + backlog review/merge

> **Status:** `in-progress`

About to: record the owner's dependabot-PR decision as a new router Q-block
(review-on-sight by the first session that sees them, then merge; major bumps =
fix-then-merge if contained, else a dedicated-session work item), put the
durable rule in its documentation home, then execute the policy on the 6 open
dependabot PRs (#1761–#1766): per-PR review (diff + changelog + real usage
grep) and merge/close/defer each to a terminal state.
