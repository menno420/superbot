# 2026-06-19 — Website split: rollout + next-steps handoff doc

> **Status:** `complete`

Owner-requested follow-up to the ultracode review session (#1122, merged). Persist the verbal
handoff as a durable, reachable doc so the next session picks up the website rollout without
re-deriving state. **Docs only — no runtime code.**

## What I'm doing
- New `docs/operations/website-split-next-steps-2026-06-19.md` (`living-ledger`): done / left / ideas
  — the rollout checklist (plan §6), the 3 review flags, the security-gated deferred slices, and the
  forward ideas (web-CI matrix, the MCP-PR conflict-guard gap, per-command status granularity).
- Anchored from `current-state.md` § Next candidates (a read-path root → `check_docs` reachable).

Born-red; flipped to `complete` as the final step.
