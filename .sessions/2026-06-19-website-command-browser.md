# 2026-06-19 — Website: interactive command browser (S1.1 + P2)

> **Status:** `in-progress`

**Run type:** `routine · ultracode`
**⚑ Self-initiated:** none (owner-dispatched ultracode build of plan units S1.1 + P2)

About to build the headline user-facing deliverable of the website two-site split:
units **S1.1** (enrich the per-command data) + **P2** (the interactive command +
feature browser), per `docs/planning/website-two-site-split-plan-2026-06-19.md`
§5 + the "Site identity & experience" brief. Foundation (S1+S2+P1) is merged (#1109);
the owner identity brief is merged (#1110).

Exclusive file set only — S1.1 touches the producer's command projection +
`check_dashboard_data`'s per-command whitelist + `site.json` + scripts tests; P2 adds
`commands.html` / `features.html` / `_command_detail.html` + the botsite test. Will NOT
edit `botsite/app.py`, other units' files, the plan doc, or `disbot/`.
