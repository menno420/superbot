# 2026-07-05 — Next-session prep + docs banking (owner-directed)

> **Status:** `in-progress` — born-red card; flips to `complete` as the deliberate final step.

## What this session is doing

Owner-directed after the save-fixes session (#1728/#1730):
1. **Bank anything useful left undocumented** into its proper repo home — the audit-seam-checker
   finding (it's the missing AST complement to the rebuild's `audit_completeness` fence, verified
   by two agents against the plan), the born-red-gate merge-race lesson, and the enforced-merge-guard
   proposal.
2. **Assess whether "finish the substrate kit next session" is the right priority** — or whether
   something is ahead of it — and write the answer as a concrete next-session handoff.
3. **Prime the repo** so the next session starts fast.

Docs/planning only — no `disbot/` runtime changes.

<!-- Did/Outcome/enders/telemetry filled in at the deliberate final flip. -->
