# 2026-07-03 — Unified layout-success simulator (owner-directed capture)

> **Status:** `in-progress` — owner-directed: capture the idea to test hub/menu layouts by
> **instruction-driven success rate** ("create roles" → does a user model reach the right node?),
> with deterministic *and* AI user models, unifying the 5 existing bespoke UX-layout sims into one
> engine over the manifest. Quantifies the "self-explanatory" half of the Q-0234 oracle; the
> mechanism behind "the sim optimizes arrangement" (Q-0230). Docs-only; no `disbot/` code.
