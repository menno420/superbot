# 2026-07-07 — Kickoff doc: owner creates `superbot-next` first

> **Status:** `complete` — small docs tweak, no gate needed for a same-day one-doc edit but
> flipped for consistency with the session-card convention.

Owner decided to create `menno420/superbot-next` manually before creating the Project, to confirm
the repo picker supports selecting multiple repos up front rather than adding one after the fact.
Updated `docs/planning/projects-eap-coordinator-kickoff-2026-07-07.md` §1 and §3 to make that the
primary path (repo already exists + already in Project scope ⇒ coordinator starts at §5 step 7,
not step 6), keeping the original coordinator-creates-it sequencing as a documented fallback.

## ⚑ Self-initiated
None — directly responsive to the owner's stated plan.

## ▶ Next action
Owner-facing: create the repo, create the Project with both repos selected, paste Custom
Instructions (§2), send the (now step-7-first) kickoff message (§3).
