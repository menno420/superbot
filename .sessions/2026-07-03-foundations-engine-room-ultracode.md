# 2026-07-03 — Foundational mechanics: the engine room (PROMPT A, runtime/logic)

> **Status:** `in-progress`
> **Branch:** `claude/ultracode-engine-room-ap2es3` · **PR:** (pending first push)
> **Session type:** ultracode discovery + audit (docs-only) — the runtime/logic half of the
> two-session foundational-mechanics brainstorm (Q-0236). A parallel session runs PROMPT B
> (presentation/verification); scopes are disjoint.

## What I'm about to do

Run PROMPT A of
`docs/planning/rebuild-foundational-mechanics-ultracode-brief-2026-07-03.md` verbatim: a
multi-agent workflow that, per runtime/logic mechanic (manifest grammar + compiler + snapshot ·
namespace registry K1 · command naming · the four-rung invocation ladder's *logic* · the command
resolver C-1 · authority + bot-owner override · audited-mutation seam + draft/preview/confirm
pipeline C-2 · compound/atomic composition · event bus + catalogue · lifecycle/tasks + injected
clock/RNG · persistence + restart-safety · cooldown/rate-limit engine C-6 · DB seam + data model +
manifest-driven selective import · settings model + presets · the portable substrate-kit),
(a) finds how it's done in the CURRENT bot with `file:line` cites, (b) researches 2–3 alternatives,
(c) pressure-tests our decided approach — then an adversarial-verify pass against shipped source
(Q-0120), a completeness-critic round (loop until two rounds add nothing), and a synthesis into a
mechanics inventory + a **ranked issues ledger** scored by the 10-class critical-review rubric.

**DOCS-ONLY:** no `disbot/` or new-repo code, no bot launch. SURFACE issues + options; do NOT
re-litigate frozen dispositions or make owner-gated decisions — flag them.

**Deliverable:** `docs/analysis/rebuild-discovery/foundations/runtime-logic-mechanics-2026-07-03.md`

## Scope boundary

Session B owns hub/navigation, panel rendering, the card + media engine, interface presets,
help/description projection, the response/result grammar, the "did you mean" *rendering*, the
rubric itself, the correctness oracle, the layout-success simulator. Where a mechanic spans both
(fuzzy matcher: logic is mine, rendering is B's) I cover only my side and note the handoff seam.

<!-- close-out (Context delta, ideas, previous-session review, friction→guard) appended before flipping to complete -->
