# Session — feature-completion assessments: Mining + Creatures (Q-0209)

> **Status:** `in-progress`

**Run type:** routine · dispatch

## What I'm about to do
Empty-fire dispatch run. S1 posture is completion-first (Q-0209); the ▶ Next offline-startable
item is assessing the remaining unassessed **games** in the feature-completion framework. This
run assesses the two unassessed games — **Mining** and **Creatures** — against
[`docs/planning/feature-completion/rubric-game.md`](../docs/planning/feature-completion/rubric-game.md),
producing a `◐ assessed` certificate (filled rubric + punch-list) for each under
`docs/planning/feature-completion/units/`, flips the ledger `State`, and re-runs the
completion scoreboard. Bugs-first: any contained, root-causable defect found during the read
(e.g. a trapped view) gets fixed in this PR with a regression guard.

(Sections below filled at close.)
