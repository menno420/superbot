# 2026-06-28 — Server-function completion assessments (Q-0209)

> **Status:** `in-progress`
> **Run type:** routine · dispatch

## What I'm about to do
Empty-fire scheduled dispatch. Every game is now `◐ assessed`; the unassessed completion-ledger
set is all server-functions. Continuing the Q-0209 completion-first arc: assess a batch of
server-function units against `rubric-server-function.md` (`▢ → ◐`), one certificate each under
`docs/planning/feature-completion/units/`, with an honest punch-list per unit. Targeting
Moderation, Economy, Roles, XP & levels. Update the ledger + regenerate the scoreboard.

Offline / read-only code inspection + docs; no `disbot/` runtime changes expected unless a
contained, root-caused bug surfaces during assessment.
