# 2026-06-19 — Fleet B5: autospec mock-fidelity guard

> **Status:** `in-progress`

About to: add `scripts/check_autospec_fidelity.py` (warn-only AST guard flagging un-`spec`'d mock setattr on `disbot.*` targets) + its unit test, per fleet unit B5.
